/** Product name — single source of truth. Renamed on Day 16; change here only. */
export const APP_NAME = "MeetingMind";

/** Backend origin (dev). Becomes dynamic when packaged as a Tauri sidecar (Day 7). */
export const API_BASE = "http://127.0.0.1:8765";
export const WS_BASE = "ws://127.0.0.1:8765";